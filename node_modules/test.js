var dbPool = require('./createDBConnectionPool');
var tableNames = require('./tableNames');
var connection = dbPool.connection;

var callback = function(results) {
    console.log(results);
}


var errorHandler = function(err) {
    console.log(err);
}

connection.connect();

var query = "select * from sc_customers";
//var query = "insert into sc_customers (customer_name, mobile_phone, email, is_registered) values ('测试3', '1130000', 'test3@msn.com', 'Y')";

connection.query(query,function(err,results){console.dir(results)});

connection.end();


